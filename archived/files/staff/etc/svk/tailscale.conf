# Staff desktops are MAIN devices — on the tailnet. Enroll on first boot using
# the reusable, staff-tagged pre-auth key on the provisioning USB (filesystem
# label SVK-PROV, file "tailscale-authkey"). svk-tailscale-enroll.service does it.
TAILSCALE_TAG="tag:svk-staff"
TAILSCALE_ENROLL="yes"
